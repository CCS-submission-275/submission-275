#include "third_party/blink/renderer/core/inspector/forensics/crypto_random_event.h"

namespace blink {

String CryptoRandomEvent::ToString() {
  String out = "FrameId: " + FrameId() + "; URL:"
          + Url() + "; Values:" + Values();
  return out;
}

} // blink
