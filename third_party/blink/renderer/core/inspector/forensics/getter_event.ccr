#include "third_party/blink/renderer/core/inspector/forensics/getter_event.h"

namespace blink {

String GetterEvent::ToString() {
  String out = "FrameId: " + FrameId() + " "
          + Interface() + "." + Attribute() + " "
          + "ReturnValue: " + ReturnValue();
  return out;
}

} // blink
